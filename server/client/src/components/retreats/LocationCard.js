import React from "react";

function OptionCard = props {

  const { name, city, state, address, zipcode, url, pictureUrls, description, drivingDescription } = props.location;

  return (

  );
}

export default OptionCard;
